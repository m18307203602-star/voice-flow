# Voice Flow — Python 依赖
# 安装: pip install -r requirements.txt

# GUI 框架
PySide6>=6.6.0

# HTTP 客户端（LLM API / 许可证 / 更新检查）
httpx>=0.27.0

# WebSocket 客户端（腾讯/阿里/讯飞 STT 流式连接）
websocket-client>=1.8.0

# Windows API（热键轮询 / COM / 剪贴板）
pywin32>=306

# 音频录制
sounddevice>=0.5.0
numpy>=1.24.0

# 剪贴板（纯文本注入）
pyperclip>=1.9.0

# 许可证签名验证
cryptography>=42.0.0

# 模拟键盘（Ctrl+C：右 Shift 录音前复制选中文字）
pyautogui>=0.9.54

# ====== 仅打包 ======
# pyinstaller>=6.0.0   # 构建 .exe 时安装：pip install pyinstaller
